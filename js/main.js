// =========================
// SMARTLEARN - MAIN
// =========================

// Tạm thời tiến trình tổng
let overallProgress = 0;

// Hiển thị tiến trình
document.getElementById("overallProgress").textContent =
    overallProgress + "%";